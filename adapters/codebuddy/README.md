# CodeBuddy / WorkBuddy Adapter Status

I0 contains no CodeBuddy or WorkBuddy configuration file because the exact local MCP/plugin manifest has not been verified from an official documentation source or a locally installed client. A generic `mcp.json` would be a fabricated vendor contract.

When an official, installed build documents local stdio MCP configuration, it should launch the same `node /absolute/path/to/src/server.js` process, set the working directory to the user workspace, and use the canonical `skills/ai-disk-doctor` folder without adding any new scanner, policy, risk, cleanup, or recovery code.
