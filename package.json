{
  "name": "@quzhiii/ai-disk-doctor-integrations",
  "version": "0.1.0-alpha.2",
  "description": "Universal local Agent Skill and non-destructive diagnostic MCP server for AI Disk Doctor",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/core.js && node --check src/schemas.js && node --check src/tools.js && node --check adapters/core/capabilities.js && node --check mcp/projection/capabilities.js && node --check mcp/server/compatibility.js && node --check mcp/tools/aidisk-capabilities.js && node --check mcp/tools/aidisk-workspace-explain.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "ajv": "8.20.0"
  }
}
